# Common
.DS_Store
.env
.env.local
.env.*.local
*.log

# Backend (Rust)
backend/target/
backend/.env

# Frontend (Node/Next.js)
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/.env
frontend/.env.local

# Instructions & Planning
# instructions/*  <-- Keep this if you actually want to ignore the instruction files, 
# although usually they are part of the project documentation.
# For now I will keep it as per your previous file if that's what you intended.
instructions/*
